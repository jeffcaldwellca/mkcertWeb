{
  "name": "mkcert-web-ui",
  "version": "1.0.0",
  "description": "Web UI middleware for managing mkcert CLI and certificate files",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "https": "ENABLE_HTTPS=true node server.js",
    "https-dev": "ENABLE_HTTPS=true nodemon server.js",
    "https-only": "ENABLE_HTTPS=true FORCE_HTTPS=true node server.js",
    "env": "node -r dotenv/config server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "mkcert",
    "certificates",
    "ssl",
    "tls",
    "web-ui",
    "middleware"
  ],
  "author": "",
  "dependencies": {
    "archiver": "^7.0.1",
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "dotenv": "^16.6.1",
    "express": "^4.19.2",
    "fs-extra": "^11.2.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
